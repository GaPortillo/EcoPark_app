// lib/core/utils/validators.dart

class Validators {
  static bool isValidEmail(String email) {
    // Implemente a lógica de validação de e-mail aqui (Gmail, Outlook, etc.)
    // ...
    return true;
  }
}
